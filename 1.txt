emit_mapClick(e, feature) {
let featureId = feature?.properties?.id || feature?.properties?.handle || feature?.properties?.chezhan_id;
if (featureId !== undefined) {

let hIdstr = feature.properties.chezhan_id;
if(feature.sourceLayer.indexOf('meso') != -1){
    hIdstr = feature.properties.id;
}

let mapGroup = "";
mapGroup = (["v_macro_stat", "v_macro_rail"].includes(feature.sourceLayer) ? "macro" : mapGroup);
mapGroup = (["meso_cc", "v_meso_rail"].includes(feature.sourceLayer) ? "meso" : mapGroup);


//车站station 区间section
let nodeType = "";
nodeType = (["meso_cc", "v_macro_stat"].includes(feature.sourceLayer) ? "station" : nodeType);
nodeType = (["v_meso_rail", "v_macro_rail"].includes(feature.sourceLayer) ? "section" : nodeType);



// console.log(feature);
this.fun_showProperty(hIdstr, '', feature.sourceLayer, feature.properties);
}